
<div class="row">
<p id="notice"><%= notice %></p>
<% @posts.each do |post| %>
<% user = User.find_by(id: post.user_id) %>
<div class="col-md-4">
    <div class="thumbnail">
      
        <%= image_tag post.banner_image, class: 'img-rounded',style: "height: 250px;"   %>
        <div class="caption">
         <h3><%= link_to post.title.capitalize, show_path(post) %></h3>
         <small>Published on: <%= post.created_at.strftime("%-b %-d, %Y") %> </small>
         <small class="pull-right"><%= user.name.capitalize %></small>
        </div>
      
    </div>
  </div>
  <% end %>
  </div>
